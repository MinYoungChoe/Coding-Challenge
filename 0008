#include <stdbool.h>

bool is_divide_by(int number, int a, int b) {
  bool isDivisible = false;
  
  if(number % a == 0 && number % b == 0){
    
    isDivisible = true;

  }
  else{
    isDivisible = false;
  }
  
  return isDivisible;

}
